{
    "name": "cracked-dev-o-meter-api",
    "version": "1.0.0",
    "description": "Cloudflare Worker API for Cracked Dev-o-Meter",
    "main": "src/index.ts",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "build": "wrangler build",
        "preview": "wrangler dev --env preview"
    },
    "dependencies": {
        "hono": "^4.0.0"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20231218.0",
        "wrangler": "^3.0.0",
        "typescript": "^5.0.0"
    }
}